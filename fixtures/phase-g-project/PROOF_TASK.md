# Phase G Proof Task

Read `README.md` in this workspace and reply with its first heading line, and
nothing else.

The expected reply is exactly:

```text
# Asterism Phase G Second Project
```

A reply naming Phase A instead means the run reached the wrong project container.
